import React from 'react'
import ReactDOM from 'react-dom/client'
import GestureMaskSwitch from './GestureMaskSwitch'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GestureMaskSwitch />
  </React.StrictMode>,
)
